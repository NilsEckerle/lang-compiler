// TODO: #include must be handled by preprocessor
// TODO: implement preprocessor
//#include <stdio.h> // use c library

int main (int argc, char *argv[]) {
  let n_times = 5; // type: const int

  for (int i = 0; i < 5; i++) { // type: mutable int
    printf("%d. Hello World\n", i+1);
  }

  return 0;
}
